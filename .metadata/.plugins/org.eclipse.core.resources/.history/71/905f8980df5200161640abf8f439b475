package cn.hsx.strategy;

public class DataSort {
	
	public static void sort(int[] a) {
		for (int i = 0; i < a.length; i++) {
			for (int j = i; j < a.length; j++) {
				if (a[i] > a[j]) {
					swap(a, i, j);
				}
			}
		}
	}
	
	public static void p(int[] arr) {
		for (int i = 0; i < arr.length; i++) {
			System.out.println(arr[i]);
		}
	}
	
	private static void swap(int[] a, int x, int y) {
		int tmp = a[x];
		a[x] = a[y];
		a[y] = tmp;
	}
	
	
	public static void sortCat(Object[] cats) {
		for (int i = 0; i < cats.length; i++) {
			for (int j = i; j < cats.length; j++) {
				/*if (cats[i].getAge() > cats[j].getAge()) {
					swapSort(cats, i , j);
				}*/
				if (((Comparable) cats[i]).compareTo((Comparable) cats[j]) == 1) {
					
				}
			}
		}
	}
	
	public static void pCat(Cat[] cats) {
		for (int i = 0; i < cats.length; i++) {
			System.out.println(cats[i].toString());
		}
	}
	
	private static void swapSort(Cat[] cats, int x, int y) {
		Cat cat = cats[x];
		cats[x] = cats[y];
		cats[y] = cat;
	}

}
